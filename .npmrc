# NPM Configuration for Houseiana Project

# Use legacy peer dependency resolution for compatibility
legacy-peer-deps=true

# Engine strict to ensure Node version compatibility
engine-strict=false

# Save exact versions
save-exact=false

# Fund messages
fund=false

# Audit level
audit-level=moderate
